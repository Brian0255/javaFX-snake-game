package sample;

//used to play the game again easily

import javafx.scene.paint.Color;

public class GameSettings {
  static double OFF_X;
  static double OFF_Y;
  //Used to adjust for top bar when resizing the scene. There is also an offset in the X direction.

  static boolean played;
  static Color headColor;
  static Color bodyColor;
  static Color fruitColor;
  static int length;
  static int height;
  static int width;
  static String name;
  static int fruits;
}
